# Base project dependencies (models, benchmark, evaluation, profiling).
# OPTIMIZATION dependencies (torch-tensorrt, onnx, onnxruntime-gpu, onnxsim)
# are NOT listed here -- they are installed directly in optimization_full.ipynb
# (or in the Modal image, see modal_runner.py).

# -- Deep Learning -------------------------------------------------------------
torch>=2.0.0
torchvision>=0.15.0     # RetinaNet R50/R101, FCOS R50 (R101 rebuilt from resnet101)

# -- Vision / detection --------------------------------------------------------
effdet                  # EfficientDet D4/D5/D6 (also pulls timm as a dependency)
timm>=0.9.0             # EfficientNet backbones for effdet

# -- Data and evaluation -------------------------------------------------------
# Windows: if pycocotools fails, use:  pip install pycocotools-windows
pycocotools
opencv-python
Pillow

# -- Numerical / utilities -----------------------------------------------------
numpy
pandas
psutil                  # free-memory detection (RAM/VRAM adaptive batch)
tqdm                    # progress bar in setup.py

# -- Profiling (baseline phase) ------------------------------------------------
tensorboard             # PyTorch Profiler export + TensorBoard visualization
nvtx                    # Nsight Systems color annotations (optional)

# -- Detectron2 ----------------------------------------------------------------
# Kept as a safeguard (R101 is now in torchvision, but we keep D2 available
# just in case). NOT installed via pip -- handled by setup.py (clone + extract).
# To install directly:
#   pip install 'git+https://github.com/facebookresearch/detectron2.git'
